<table class="table table-striped table-hover table-condensed">
  <tr>
    <th class="center">Image</th>
    <th class="center">House</th>
    <th class="center">House Descript</th>
    <th class="center">Rental</th>
    <th class="center">Action</th>
  </tr>

  <% @rentings.each do |renting| %>
    <tr>
      <td> <%= image_tag renting.image.url(:thumb) %></td>
      <td> <%= renting.name %></td>
      <td> <%= renting.address %></td>
      <td> <%= number_to_currency(renting.rental) %></td>
      <td> <%= number_to_currency(renting.rental) %></td>
      <td> <%= renting.created_at.strftime("%Y %ˆb %d") %></td>
    </tr>
</table>
          <div class="btn-group">
            <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
              Action <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "View", renting, class: "btn btn-default" %></li>
              <li><%= link_to "Edit", edit_renting_path(renting) %></li>
              <li><%= link_to 'Delete', renting, method: :delete, data: { confirm: 'Are you sure you want to delete the house?'} %></li>
            </ul>
          </div>        
      </td>
    </tr>
  <% end %>
</table><br>